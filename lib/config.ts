const config = {
  // Add any global configuration variables here
  apiUrl: process.env.NEXT_PUBLIC_API_URL || "http://localhost:3000/api",
  // Add more configuration variables as needed
}

export default config

